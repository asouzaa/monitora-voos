[project]
name = "monitora-voo"
version = "0.2.0"
description = "Monitor de preços de voos de Belém para Recife e Fortaleza."
requires-python = ">=3.11"
dependencies = [
    "faster-flights==3.7.0",
]

[tool.unittest]
start-dir = "tests"
